class DatePicker {
  constructor() {
    // this.date
  }
  init() {
    // this.options = options
    this.date.setDate(1);
    this.createMonth();
    this.createListeners();
  }
}

export default DatePicker;
