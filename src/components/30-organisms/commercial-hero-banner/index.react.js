import withReact from '../../../utils/with-react';
import AXACommercialHeroBanner from './index';

export default (createElement, version) => withReact(createElement, AXACommercialHeroBanner, version);
