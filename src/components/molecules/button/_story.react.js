/* global document */
import { storiesOf } from '@storybook/html';
import './src/index';

storiesOf('Button', module).add('Button React', () => '<axa-button motion>Im a button with react</axa-button>');
